[project]
name = "spotify"
version = "0.1.0"
description = ""
authors = [
    {name = "Andreas Bazigos",email = "bazigosa@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyqt6[multimedia] (>=6.9.1,<7.0.0)",
    "mutagen (>=1.47.0,<2.0.0)"
]

[tool.poetry]
package-mode = false

[tool.poetry.group.dev.dependencies]
requests = "^2.32.4"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
